package com.zerofriction.localcast.ui.theme

import androidx.compose.material3.Typography

/** Default Material 3 type scale; revisit if the product develops a type identity. */
val AppTypography = Typography()
